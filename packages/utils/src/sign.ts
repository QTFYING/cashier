/**
 * 签名工具类
 * (Placeholder)
 */
export const sign = (data: Record<string, any>, secret: string): string => {
  // TODO: Implement actual signing logic or move from original location if it exists
  return 'mock_sign_' + Date.now();
};
